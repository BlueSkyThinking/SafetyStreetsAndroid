<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    tools:context="com.code4piter.blueskythinking.megapp.ui.activity.PlaceActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:layout_marginTop="4dp" />

        <ImageView
            android:id="@+id/preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:contentDescription="@string/cameraPreviewCD"
            android:elevation="@dimen/elevation"
            android:padding="@dimen/photoPreviewPadding"
            android:scaleType="centerCrop"
            tools:src="@android:mipmap/sym_def_app_icon" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

            <TextView
                android:id="@+id/dangerLevel"
                style="@style/detailInfoTextView"
                tools:text="text" />

            <TextView
                android:id="@+id/name"
                style="@style/detailInfoTextView"
                tools:text="text" />

            <TextView
                android:id="@+id/address"
                style="@style/detailInfoTextView"
                tools:text="text" />
        </LinearLayout>


    </LinearLayout>

</ScrollView>
